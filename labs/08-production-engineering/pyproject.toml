[project]
name = "production-engineering-lab"
version = "0.1.0"
description = "Offline production topology, release and rollback contracts"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
