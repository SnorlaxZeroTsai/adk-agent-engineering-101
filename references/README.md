# References

This directory stores reproducibility metadata and evidence indexes, not copies
of upstream repositories.

- `upstream-lock.yaml`: immutable commits used by the current research snapshot.
- `source-index.md`: important source files, symbols and why they matter.

Re-clone upstream repositories when a new phase needs source inspection, then
record any intentional version update in the roadmap and `PROJECT_STATE.md`.
