# Mini Agent Garden

The prototype will validate, rather than merely describe, the proposed
blueprint abstractions.

Planned capabilities:

```text
list -> inspect -> create -> eval -> run
```

Implementation starts only after the blueprint schema has evidence-backed field
semantics and at least three architecture patterns are executable.
