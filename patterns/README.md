# Pattern Catalog

Pattern documents will use one shared template:

`Problem -> Architecture -> When to use -> When NOT to use -> Why ->
Alternatives -> Trade-offs -> Failure modes -> ADK implementation ->
Primary sources -> Minimal example`.

No pattern is considered complete until it has:

1. source evidence,
2. a competing alternative,
3. an executable experiment,
4. at least one intentional failure case.
